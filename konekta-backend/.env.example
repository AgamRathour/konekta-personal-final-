# MongoDB Configuration
# Use IPv4 address to avoid IPv6 localhost issues; for local dev use:
MONGO_URI=mongodb://127.0.0.1:27017/konekta
# If you use MongoDB Atlas use the provided connection string instead.

# JWT Configuration
JWT_SECRET=your_super_secret_jwt_key_here_change_this
JWT_EXPIRE=7d

# Frontend URL (for CORS)
FRONTEND_URL=http://localhost:5175

# Server Configuration
PORT=5000
NODE_ENV=development
